---
import Layout from "@/layouts/Layout.astro";
import GlassHeader from "@/components/GlassHeader";
import Footer from "@/components/Footer";
import { getInterviewEntries } from "@/lib/interviews";

const interviews = await getInterviewEntries();

function interviewIntroductionItems(introduction: string, fallback: string) {
  return (introduction || fallback)
    .split(/\r?\n/)
    .map((line) => line.trim())
    .filter((line) => line.length > 0);
}
---

<Layout
  title="Interviews - Yangming Guo"
  description="Interview transcripts with segmented audio playback."
>
  <GlassHeader client:load />
  <main class="min-h-screen">
    <section class="py-12 md:py-16">
      <div class="container max-w-3xl mx-auto px-6 md:px-4">
        <div class="interview-index-heading mb-10">
          <p class="interview-rights-notice">
            版权说明：本页仅为个人学习记录。原始访谈内容的著作权及相关权利归原权利人所有。本站不提供商业使用，不替代原始发布渠道，不主张任何原始内容权利。如涉及权利问题，请联系我处理。
          </p>
        </div>

        {
          interviews.length === 0 ? (
            <div class="rounded-md border border-border/50 bg-background/80 backdrop-blur-md px-3 py-2 text-muted-foreground">
              No interviews yet.
            </div>
          ) : (
            <div class="interview-card-list">
              {interviews.map((interview) => (
                <a href={interview.href} class="interview-card">
                  <span class="interview-card-header">
                    <span class="interview-card-title">
                      {interview.title}
                    </span>
                  </span>
                  <span class="interview-card-body">
                    <span class="interview-card-introduction">
                      {interviewIntroductionItems(interview.introduction, interview.slug).map((line) => (
                        <span class="interview-card-introduction-item">{line}</span>
                      ))}
                    </span>
                  </span>
                </a>
              ))}
            </div>
          )
        }
      </div>
    </section>
  </main>
  <Footer client:load />
</Layout>

<style>
  .interview-card-list {
    display: grid;
    gap: 0.9rem;
  }

  .interview-card {
    background: color-mix(in oklab, var(--background) 86%, transparent);
    border: 1px solid color-mix(in oklab, var(--muted-foreground) 26%, var(--border));
    border-radius: 0.5rem;
    display: flex;
    flex-direction: column;
    min-height: 0;
    overflow: hidden;
    text-decoration: none;
    box-shadow: 0 0.45rem 1.2rem color-mix(in oklab, var(--foreground) 5%, transparent);
    transition:
      background-color 0.2s ease,
      border-color 0.2s ease,
      box-shadow 0.2s ease,
      transform 0.2s ease;
  }

  .interview-card:hover,
  .interview-card:focus-visible {
    background: color-mix(in oklab, var(--color-purple-500) 5%, var(--background));
    border-color: color-mix(in oklab, var(--color-purple-500) 32%, var(--border));
    box-shadow: 0 1.1rem 2.6rem color-mix(in oklab, var(--color-purple-500) 12%, transparent);
    transform: translateY(-0.16rem);
  }

  .interview-index-heading .interview-rights-notice {
    margin-top: 0;
  }

  .interview-card-header {
    align-items: center;
    background: linear-gradient(
      90deg,
      color-mix(in oklab, var(--color-purple-500) 7%, transparent),
      color-mix(in oklab, var(--color-pink-500) 7%, transparent)
    );
    border-bottom: 1px solid color-mix(in oklab, var(--border) 58%, transparent);
    display: flex;
    min-height: 3.75rem;
    padding: 0.75rem 1.25rem;
    transition: background 0.2s ease;
  }

  .interview-card:hover .interview-card-header,
  .interview-card:focus-visible .interview-card-header {
    background: linear-gradient(
      90deg,
      color-mix(in oklab, var(--color-purple-500) 11%, transparent),
      color-mix(in oklab, var(--color-pink-500) 11%, transparent)
    );
  }

  .interview-card-body {
    min-width: 0;
    padding: 1.05rem 1.25rem 1.15rem;
  }

  .interview-card-title {
    color: color-mix(in oklab, var(--foreground) 76%, transparent);
    display: block;
    font-size: 1.15rem;
    font-weight: 650;
    line-height: 1.35;
    overflow-wrap: anywhere;
    transition: color 0.2s ease;
  }

  .interview-card:hover .interview-card-title,
  .interview-card:focus-visible .interview-card-title {
    color: var(--color-purple-500);
  }

  .interview-card-introduction {
    color: var(--muted-foreground);
    display: grid;
    font-size: 0.92rem;
    gap: 0.26rem;
    line-height: 1.36;
    overflow-wrap: anywhere;
  }

  .interview-card-introduction-item {
    background: color-mix(in oklab, var(--muted) 42%, transparent);
    border: 1px solid color-mix(in oklab, var(--border) 68%, transparent);
    border-radius: 0.42rem;
    display: grid;
    font-family: "PingFang SC", "Hiragino Sans GB", "Microsoft YaHei", sans-serif;
    gap: 0.38rem;
    grid-template-columns: auto minmax(0, 1fr);
    padding: 0.26rem 0.5rem;
  }

  .interview-card-introduction-item::before {
    background: color-mix(in oklab, var(--color-purple-500) 62%, var(--muted-foreground));
    border-radius: 999px;
    content: "";
    height: 0.28rem;
    margin-top: 0.55em;
    width: 0.28rem;
  }
</style>
